apiVersion: v1
kind: Namespace
metadata:
  name: photoprism-FAMILY_NAME
  labels:
    app: photoprism
    family: FAMILY_NAME
    managed-by: ai-ops-agent
    istio-injection: enabled  # Service mesh for zero-trust
